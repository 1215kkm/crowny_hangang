---
description: 강팀이 프로젝트를 스캔해 안건을 자동 생성하고, 각 멤버 회의 → 요약 → 텔레그램 전송까지 한 번에 수행
---

# /회의시작

CEO가 "회의 시작해" 또는 `/회의시작 [주제]` 라고 하면 강팀장이 아래 프로토콜을 *자동 수행*한다.

주제(`$ARGUMENTS`)가 비어 있으면 강팀장이 *프로젝트를 스캔*해서 가장 시급한 주제 1개를 직접 정한다.

---

## 자동 회의 프로토콜 (강팀장 진행)

### 1) 회의 폴더 생성

```bash
bash ~/.claude/bin/start-meeting.sh "$ARGUMENTS"
# 인자 없으면 강팀장이 주제를 한 줄로 정한 뒤 다시 실행
```

### 2) 프로젝트 스캔 → 안건 생성

강팀장은 *동시에* 다음을 수집한다 (병렬 Bash·Read 호출):
- `git log -20 --oneline` — 최근 변경
- `git diff HEAD~5 --stat` — 변경 범위
- `ls .ai-team/meetings/ 2>/dev/null | tail -5` — 최근 회의록
- `find . -name "*.md" -path "*/team-memory/*"` — 전역 브레인에 누적된 학습
- `ls src/ app/ pages/ 2>/dev/null` — 코드 구조
- `git log --since="2 weeks ago" --pretty=format:"%s"` — 최근 작업 흐름

위 정보로 *4개 영역*에 대해 *각 1~2개씩* 안건을 뽑는다:
- 🧪 **UX 개선** — 사용자가 막힐 만한 흐름·이탈 지점
- 🎨 **UI 개선** — Crowny Class 토큰 적용 누락·디자인 부채
- 💡 **새 아이디어 (수익)** — 지속 사용 + 심플 수익으로 연결 가능한 한 가지
- 🛡️ **보안·리스크** — 인증·결제·개인정보·API 키 노출
- 🔍 **품질** — 최근 회귀·미테스트 영역 (선택)

### 3) 안건 작성 — `meeting.html` ACT 1

**대본형 템플릿** 사용 (`templates/meeting.html`). 인물 성격·말투는 [`personas.md`](../knowledge/team-memory/personas.md) 필수 선행 읽기.

`{{TITLE}}`/`{{DATE}}`/`{{AGENDA}}` 채운 뒤, `<!-- ACTS_START -->` 바로 아래에 ACT 1 블록을 Edit으로 append:

```html
<div class="actblock" id="act1">
  <div class="act"><div class="no">ACT 1</div><h2>안건</h2></div>
  <div class="dir">지문(상황). 예: 오늘 회의 안건을 강팀장이 연다.</div>

  <div class="l daepyo"><span class="who">강팀장</span>"오늘 안건 [N]개. [영역] 부터 보자." <span class="think">(속마음 — 캐릭터 시트대로)</span></div>
</div>
```

각 안건마다 새 ACT(`id="act2"`, `id="act3"`...)로 이어간다. 참석자·목표는 첫 지문(`.dir`)에 한 줄로.

### 4) 각 멤버 발언 — Agent 도구로 *병렬* 호출

각 안건마다 *관련 역할*만 부른다 (전원이 모든 안건에 의견 낼 필요는 없음):

| 안건 영역 | 호출 에이전트 |
|---|---|
| UX 개선 | `designer`, `qa` |
| UI 개선 | `designer` |
| 새 아이디어 (수익) | `marketer` |
| 보안·리스크 | `qa` |
| 품질 | `qa` |

> 5인 체제: `designer`(강디)가 UX+UI를, `qa`(강체크)가 품질+보안을, `marketer`(아뱅)가 마케팅+심리/수익 아이디어를 한 몸으로 담당. `pm`(강팀장)이 진행·라우팅.

#### ⚠️ 선택 참석자 — 강톡 (`interaction-designer`)

**강톡는 위 표에 없다. 기본 참석자가 아니다.**

강팀장은 **CEO 가 강톡를 명시적으로 부른 회의에서만** `interaction-designer` 를 호출한다.
안건이 UX·UI 처럼 보인다는 이유로 **강팀장 판단으로 부르는 것은 금지**다.

| 호출 신호 (이게 있어야만 부름) | 예 |
|---|---|
| `/회의시작` 인자에 이름이 있음 | `/회의시작 "좌석 배치 개편" 강톡` |
| CEO 가 회의 도중 지명 | "강톡 불러", "강톡 의견도", "@강톡" |
| CEO 가 행동 검사를 요청 | "인터랙션 봐줘", "행동 검사해", "동작 결함 찾아" |

- 신호가 있으면 → 안건 목록의 **참석자 줄에 `강톡` 를 추가**하고, `agent-brief.sh interaction-designer "<안건>"` 을 붙여 호출.
- **호출 순서**: 강디(시안) → **강톡(행동 검사)** → 강개발. 강톡는 *강디의 시안을 입력으로 받는다* — 시안 없이 먼저 부르지 않는다.
- 강톡 발언 클래스는 `gtok`, 닉네임은 `강톡`.
- 신호가 없으면 → 강톡는 그 회의에 **존재하지 않는다.** 참석자 줄에도 쓰지 않는다.

각 에이전트 호출 시 프롬프트에 다음 포함 (**컨텍스트 슬림화** — 하네스 베스트프랙티스 "지도만 전달"):
- "이번 회의 폴더: `.ai-team/meetings/[date-slug]/meeting.html`"
- "안건: [해당 안건]"
- "프로젝트 컨텍스트: [스캔 결과 요약 3~5줄]"
- **호출 직전 `bash scripts/agent-brief.sh <agent-id> "<해당 안건>"` 결과 전체** — 지도(역할 한 줄·금기·응답 형식) + 활성 스타일 + 키워드 매칭 발췌 + 이전 강체크 지적(있으면) + 마지막 테스트 출력(있으면).
- "발언 형식: 해당 ACT 블록의 `.actblock` 안, `.sum` 정리 박스 *앞*에 `<div class="l [role]"><span class="who">[닉네임]</span>"대사." <span class="think">(속마음)</span></div>` 줄을 Edit으로 append. `[role]` = daepyo/gdi/gdev/gchk/abang (강팀장/강디/강개발/강체크/아뱅) + 호출된 경우에만 gtok(강톡). 시각 자료(SVG/표/Mermaid/코드)가 필요하면 대사 다음 줄에 이어 붙임."
- "말투·속마음은 [`personas.md`](../knowledge/team-memory/personas.md) 캐릭터 시트 그대로 — 강팀장 짧고 묵직, 강디 자신만만, 강개발 직설, 강체크 까칠, 아뱅 긍정·호전적."
- "전원 찬성 금지 — 최소 1명 가벼운 반대·비판."
- "전체 에이전트 정의 .md 를 매번 통째로 보지 말 것 — 필요한 디테일만 Read."

여러 에이전트는 *동시에* 호출 (한 메시지에 multiple Agent tool uses).

### 4.5) 강체크 검증 루프 (강개발 산출물 있을 때 — Ralph Wiggum Loop)

상세: [`.claude/knowledge/qa/feedback-loop.md`](../knowledge/qa/feedback-loop.md)

요약:
1. 강개발 발언 후 → 강체크 자동 호출 (위 4번 패턴 + `agent-brief.sh qa`)
2. 강체크 응답 끝의 "검토 결과:" 줄 파싱:
   - `통과 가능` → 루프 종료
   - `조건부 통과` / `보류 권고` → 강개발 *재호출* (최대 2회, 매 호출 컨텍스트에 `last-qa-findings.txt` 자동 포함)
3. 2회 후에도 🔴 남으면 → SUMMARY "미결정" 에 자동 박힘 + CEO 결정 요청
4. 회의 마감 시 `> .ai-team/last-qa-findings.txt` 로 비우기 (다음 회의 잔재 방지)

### 5) 디자인 안건이 있었으면 — 강디 mockup 채움

`designer` 에이전트를 한 번 더 호출해 `mockup.html`의 `<!-- SCREENS_START -->` 안에 *논의된 화면을 grid 한 페이지로* 채우게 한다.

### 6) 각 ACT 마감 — `.sum` 정리 박스

안건(ACT) 하나가 끝나면 강팀장이 그 `.actblock` 안 마지막에 정리 박스 append:

```html
<div class="sum"><b>정리</b>
  <ul><li>[안건] 결정 한 줄</li><li>다음 액션 한 줄</li></ul>
</div>
```

### 7) 강팀장 마감 요약 (텔레그램 발송용)

`<!-- SUMMARY_START -->` ~ `SUMMARY_END -->` 사이 텍스트(주석, 화면엔 안 보임)를 다음 형식으로 채운다:

```
결정:
- [안건명] → 결정 사항 한 줄

미결정 / 다음 회의 안건:
- 이유 한 줄

다음 액션:
- [담당] 무엇을 / 언제까지 / 게이트(예: 배포 전 텔레그램 승인)
```

마무리 지문(`.end`)도 한 줄 채운다 (예: "강팀장이 노트북을 덮는다. \"수고했어.\"").

### 8) 상태 저장 → 텔레그램 전송

```bash
# 상태 파일 업데이트 (있으면)
bash ~/.claude/bin/update-state.sh --stage="planned" --meeting="$DIR"

# 텔레그램 전송
bash ~/.claude/bin/send-meeting.sh "$DIR"
```

전송 후 강팀장은 사용자에게 한 줄 보고:
> "회의록 텔레그램으로 보냈습니다. 텔레그램에서 `/진행` 또는 여기서 '진행해' 라고 하시면 다음 단계로 갑니다."

---

## 강팀장 가이드라인

- **안건은 4~5개 이하.** 한 회의 = 한 결정. 더 많이 잡으면 결정 흐려짐.
- **수익 경로 한 줄 박기.** 아뱅의 9번 원칙 "Revenue-bound" — 안건마다 *돈이 어디서 나오는지* 한 줄.
- **CTA·디자인 토큰 위반은 강디가 자동 반려** + **PreToolUse 훅(`pre-tokens-edit.sh`)이 코드 차원에서 차단.** 강팀장이 그걸 받아서 반려 사유 한 줄로 닫는다.
- **회의 길이는 짧게.** 한 안건당 3~5턴이면 충분.
